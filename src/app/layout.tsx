import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PulseResp - Surveillance Physiologique en Temps Réel",
  description: "Application de surveillance de la fréquence cardiaque et respiratoire utilisant la technologie rPPG via la caméra",
  keywords: ["rPPG", "fréquence cardiaque", "fréquence respiratoire", "surveillance physiologique", "caméra"],
  authors: [{ name: "PulseResp Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
